name: core_ui
description: core_ui package.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.1.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  file_picker: ^6.1.1
  flutter:
    sdk: flutter
  bitmap: ^0.2.0

  data:
    path: ../data

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
